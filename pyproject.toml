[project]
name = "midas-cn"
version = "0.1.0"
description = "A-share trading decision system scaffold."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
kline = ["akshare"]

[project.scripts]
midas-cn = "midas_cn.interfaces.cli:main"

[tool.setuptools.packages.find]
include = ["midas_cn*"]
